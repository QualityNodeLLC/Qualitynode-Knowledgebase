include: package:lints/recommended.yaml

analyzer:
  exclude:
    - "**.g.dart"
    - "**.freezed.dart"
  errors:
    invalid_annotation_target: ignore

linter:
  rules:
    prefer_relative_imports: true
    avoid_print: false
